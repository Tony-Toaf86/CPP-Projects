{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar y ejecutar C++",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "g++ -g \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\" && \"${fileDirname}/${fileBasenameNoExtension}\""
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compila el archivo C++ activo y lo ejecuta automáticamente"
        }
    ]
}
